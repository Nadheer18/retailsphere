---
- name: Install nginx
  apt:
    name: nginx
    state: present

- name: Start and enable nginx
  service:
    name: nginx
    state: started
    enabled: yes

- name: Deploy nginx homepage
  template:
    src: index.html.j2
    dest: /var/www/html/index.html
  notify: Restart Nginx
