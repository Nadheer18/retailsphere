apiVersion: v1
kind: Service
metadata:
  name: retailsphere-backend
  namespace: retailsphere

spec:
  selector:
    app: retailsphere-backend

  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP

  type: ClusterIP
